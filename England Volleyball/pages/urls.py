from django.conf.urls import url, include
from django.urls import path
#from .views import IndexView, DetailView, ClubView

app_name = 'pages'

urlpatterns = [
    #path('<slug:page_slug>', ClubView.as_view(), name='club'),
]
